# Цель
Устранить падение `screen_widgets.js` с `Uncaught SyntaxError: Unexpected token ')'`, восстановить корректную загрузку payload экрана и проверить поддержку виджетов `school_news`/`rss_news` в текущей системе карусели.

# Контекст
- Входные данные: логи консоли с ошибкой парсинга JS и 403 на `/api/screen/mobil` после поломки фронтенд-скрипта.
- Ограничения: сохранить совместимость со старыми браузерами ТВ, не переносить бизнес-логику в UI.
- Ожидаемый артефакт: рабочий `static/screen_widgets.js` без синтаксических ошибок и без конфликтных merge-вставок.

# План
1. Локализовать источник синтаксической ошибки в `static/screen_widgets.js`.
2. Удалить конфликтный/дублированный блок функции `buildRssNews` и восстановить корректные скобки.
3. Проверить вызовы `renderWidgetHtml` в карусели, убрать лишние аргументы после merge.
4. Прогнать синтаксические и sanity-проверки.
5. Обновить служебные `agents.txt` и зафиксировать аудит.

# Изменения (по файлам)
- `static/screen_widgets.js`:
  - удалён дублирующий/конфликтный фрагмент `buildRssNews(...)`, из-за которого не закрывался блок функции и ломался парсер;
  - восстановлена закрывающая `}` у основной функции рендера RSS-виджета;
  - исправлен вызов `renderWidgetHtml(...)` в карусели (убран лишний параметр `d.rss_news || []`);
  - убрано дублирование ключей `rssNews` в словаре `TV_UI`.
- `static/agents.txt`:
  - добавлено правило для post-merge проверки: единичность реализаций рендереров виджетов и валидность JS до выката.
- `docs/codex/2026-04-22T02-02-44_Действие_Фикс_screen_widgets_и_карусели_news.md`:
  - создан текущий файл аудита.

# Решения/обоснования
- Ошибка `Unexpected token ')'` была следствием merge-пересечения: в файле остались две версии `buildRssNews`, одна из них вставлена внутрь другой без закрывающей скобки.
- Исправление сделано точечно в `static/screen_widgets.js`, без изменения контрактов API и без вмешательства в backend.
- Логику работы `school_news`/`rss_news` через карусель сохранена: виджеты рендерятся тем же `renderWidgetHtml`, участвуют в `childWidgetIds`, а длительность/порядок/включение остаются в существующем механизме карусели и конфигурации виджетов.

# Риски
- Остались потенциальные merge-риски в соседних фронтенд-файлах при следующих слияниях (если не запускать авто-проверки синтаксиса).
- 403 на `/api/screen/mobil` может повторяться при ошибках авторизации/параметров запроса отдельно от JS (не часть текущего патча).

# Следующие шаги
1. Добавить в CI минимальную проверку `node --check static/screen_widgets.js static/screen.js`.
2. Прогнать ручной smoke на целевом стенде экрана: загрузка payload, ротация карусели, показ `school_news` и `rss_news`.
3. При необходимости отдельно разобрать причину 403 (права/метка экрана/класс доступа).

# DoD (проверка)
- [x] `screen_widgets.js` проходит синтаксическую проверку.
- [x] Конфликтный дубль функции удалён.
- [x] Вызов рендера карусели приведён к валидной сигнатуре.
- [x] Аудит действия создан в `docs/codex/`.

# Мини-ревью (альтернативы)
1. Полный откат `static/screen_widgets.js` к предыдущему коммиту — отвергнуто, т.к. потерялись бы последние изменения по emergency countdown.
2. Частичный cherry-pick старой функции без чистки текущего файла — отвергнуто, повышает риск повторного конфликта.
3. Текущее решение (точечная чистка merge-артефактов) — выбрано как минимально инвазивное.

# Откат
- Git-откат файла: `git checkout -- static/screen_widgets.js static/agents.txt docs/codex/2026-04-22T02-02-44_Действие_Фикс_screen_widgets_и_карусели_news.md`
