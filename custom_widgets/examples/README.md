# Примеры custom-виджетов

Файлы в этой папке **не загружаются** автоматически.

Скопируйте пример в `../` (корень `custom_widgets/`) или укажите `GUARDSCHOOL_CUSTOM_WIDGETS_DIR`.

Требуется `GUARDSCHOOL_DEPLOYMENT_MODE=local`.
