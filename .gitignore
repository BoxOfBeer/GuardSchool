__pycache__/
.venv/
data/

# Build / distribution artifacts (source repo; keep binaries out of Git)
dist/
build/
build_full/
full/

# Локальные бэкапы/диффы (не в репозиторий)
backups/

# Облако guarddoc.ru: секреты (каркас см. cloud_guarddoc/)
cloud_guarddoc/.env
cloud_guarddoc/*.local

# Локальный SSH: копия ssh-local.example.bat → ssh-local.bat (пароли/хост не в git)
ssh-local.bat
cloud_guarddoc/ssh-local.bat
